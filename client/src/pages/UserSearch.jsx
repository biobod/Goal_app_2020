import React from 'react';
import { Input, Table, Button } from '@material-ui/core';
import PropTypes from 'prop-types';

const UserSearch = (props) => (
  <div>
    User search page
  </div>
);

UserSearch.propTypes = {

};

export default UserSearch;
